### Spring Boot with Selenium
Simple demo of how to configure Spring Boot together with selenium (page object pattern)  

### Setup
Lombok plugin required

> To run the tests 

```shell
$ mvn clean test
```
